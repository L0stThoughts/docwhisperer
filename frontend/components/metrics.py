"""RAGAS metrics display stub

This module will provide helpers to display evaluation metrics computed by
RAGAS in the Streamlit UI, including tables, charts, and drilldowns for
individual examples.

Planned public functions:
- render_metrics(metrics: dict)
"""
from typing import Dict


def render_metrics(metrics: Dict) -> None:
    """Render evaluation metrics in Streamlit.

    Args:
        metrics: dict of evaluation metrics produced by ragas_eval

    Note: Stub — real implementation will produce charts and interactive
    tables for metric exploration.
    """
    raise NotImplementedError
